name: fan_floating_menu
description: A customizable floating action button menu that will pop up fabs with special animation.
version: 0.0.3
homepage: https://github.com/marjandn/fan_floating_menu

environment:
  sdk: ">=2.19.1 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
